# NLP
--extra-index-url https://download.pytorch.org/whl/cpu
regex>=2023.12.25
openpyxl>=3.1.2
python-dateutil>=2.8.2
pytest>=7.3.0
requests>=2.31.0
pytz>=2024.1
spacy>=3.5.0
transformers>=4.30.0
torch
scikit-learn>=1.3.0
# collections2 is likely a typo for collections (stdlib) or a very specific package. 
# Including it if actually needed, but commenting out if it fails.
# collections2>=0.3.0
